import React from "react";
import "./Header.css";

const Header = () => (
  <header>
    <a href=".">
      <h1>Could you live on that?</h1>
    </a>
  </header>
);

export default Header;
